fastapi
uvicorn[standard]
sqlalchemy
asyncpg
pydantic
pydantic-settings
python-jose[cryptography]
bcrypt
email-validator
python-multipart
httpx
qdrant-client
langchain
langchain-community
langchain-text-splitters
pypdf
python-dotenv

redis
aiofiles
pytest
pytest-asyncio
black
flake8
mypy

# Multimodal PDF processing
PyMuPDF
opencv-python-headless
pytesseract
# paddleocr<3 uses np.sctypes and cannot import under numpy 2.x
paddlepaddle>=3.0
paddleocr>=3.1
camelot-py
pdfplumber
# Pinned: this is the version running in production. Unpinned, a docling
# release can swap the extraction pipeline under us — 2.118 already runs its
# layout model through torch.compile, which needs TORCHDYNAMO_DISABLE=1 on a
# compiler-less runtime image (see docker-compose.yml).
docling==2.118.0
# Enables Docling's RapidOCR (ONNX backend). Without it Docling's OCR engine
# fails to initialize ("rapidocr cannot be used because onnxruntime is not
# installed" -> "Unsupported configuration: torch.PP-OCRv6.det.small"), which
# forced Docling to fall back to traditional extraction.
# used plain onnxruntime (CPU build) — correct for this CPU host. 
# If you later move to a GPU box and want ONNX on GPU, that'd be onnxruntime-gpu instead (don't install both).
onnxruntime

# Improved embeddings and retrieval
sentence-transformers
rank-bm25
ftfy  # repairs UTF-8/CP1252 mojibake in extracted/OCR text
rapidfuzz  # fuzzy similarity for table validation / dedup
transformers
torch
